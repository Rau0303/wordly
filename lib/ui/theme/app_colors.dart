import 'package:flutter/material.dart';

class AppColors {
  // Основной цвет приложения
  static const Color primary = Color(0xFFFD232D);

  // Элементы интерфейса
  static const Color graphite = Color(0xFF302F2D);
  static const Color darkGray = Color(0xFF202020);
  static const Color lightGray = Color(0xFFF5F5F5);

  // Фон
  static const Color blackBackground = Color(0xFF000000);
  static const Color whiteBackground = Color(0xFFFFFFFF);
}
